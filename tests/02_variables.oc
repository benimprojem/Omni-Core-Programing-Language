fn main() {
    var x: i32 = 10;
    let y: i32 = 20; // Immutable
    const Z: i32 = 30;
    
    x = x + 5;
    // y = 25; // Hata vermeli
    
    println("Test 02: x={x}, y={y}, Z={Z}");
}