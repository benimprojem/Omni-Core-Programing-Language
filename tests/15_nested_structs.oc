struct Vec { x: f64; y: f64; }
struct Player { pos: Vec; health: i32; }

fn main() {
    var pl: Player = Player {
        pos: Vec { x: 10.0, y: 5.0 }
        health: 100
    };
    println("Player Pos: {pl.pos.x.2}, {pl.pos.y.2}");
}

